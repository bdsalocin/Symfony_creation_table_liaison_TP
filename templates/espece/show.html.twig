{% extends 'base.html.twig' %}

{% block title %}Espece{% endblock %}

{% block body %}
    <h1>Espece</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espece.id }}</td>
            </tr>
            <tr>
                <th>NomEspece</th>
                <td>{{ espece.nomEspece }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espece_index') }}">back to list</a>

    <a href="{{ path('app_espece_edit', {'id': espece.id}) }}">edit</a>

    {{ include('espece/_delete_form.html.twig') }}
{% endblock %}
