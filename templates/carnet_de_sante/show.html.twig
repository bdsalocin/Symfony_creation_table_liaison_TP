{% extends 'base.html.twig' %}

{% block title %}CarnetDeSante{% endblock %}

{% block body %}
    <h1>CarnetDeSante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carnet_de_sante.id }}</td>
            </tr>
            <tr>
                <th>NumeroCarnet</th>
                <td>{{ carnet_de_sante.numeroCarnet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_carnet_de_sante_index') }}">back to list</a>

    <a href="{{ path('app_carnet_de_sante_edit', {'id': carnet_de_sante.id}) }}">edit</a>

    {{ include('carnet_de_sante/_delete_form.html.twig') }}
{% endblock %}
