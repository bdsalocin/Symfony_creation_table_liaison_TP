<form method="post" action="{{ path('app_carnet_de_sante_delete', {'id': carnet_de_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carnet_de_sante.id) }}">
    <button class="btn">Delete</button>
</form>
