{% extends 'base.html.twig' %}

{% block title %}Edit Cage{% endblock %}

{% block body %}
    <h1>Edit Cage</h1>

    {{ include('cage/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_cage_index') }}">back to list</a>

    {{ include('cage/_delete_form.html.twig') }}
{% endblock %}
