{% extends 'base.html.twig' %}

{% block title %}Cage{% endblock %}

{% block body %}
    <h1>Cage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cage.id }}</td>
            </tr>
            <tr>
                <th>Fonctionnalite</th>
                <td>{{ cage.fonctionnalite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cage_index') }}">back to list</a>

    <a href="{{ path('app_cage_edit', {'id': cage.id}) }}">edit</a>

    {{ include('cage/_delete_form.html.twig') }}
{% endblock %}
