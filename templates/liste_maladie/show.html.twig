{% extends 'base.html.twig' %}

{% block title %}ListeMaladie{% endblock %}

{% block body %}
    <h1>ListeMaladie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_maladie.id }}</td>
            </tr>
            <tr>
                <th>NomMaladie</th>
                <td>{{ liste_maladie.nomMaladie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_maladie_index') }}">back to list</a>

    <a href="{{ path('app_liste_maladie_edit', {'id': liste_maladie.id}) }}">edit</a>

    {{ include('liste_maladie/_delete_form.html.twig') }}
{% endblock %}
