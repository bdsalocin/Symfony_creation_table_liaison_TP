{% extends 'base.html.twig' %}

{% block title %}ListeMaladie index{% endblock %}

{% block body %}
    <h1>ListeMaladie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomMaladie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for liste_maladie in liste_maladies %}
            <tr>
                <td>{{ liste_maladie.id }}</td>
                <td>{{ liste_maladie.nomMaladie }}</td>
                <td>
                    <a href="{{ path('app_liste_maladie_show', {'id': liste_maladie.id}) }}">show</a>
                    <a href="{{ path('app_liste_maladie_edit', {'id': liste_maladie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_liste_maladie_new') }}">Create new</a>
{% endblock %}
