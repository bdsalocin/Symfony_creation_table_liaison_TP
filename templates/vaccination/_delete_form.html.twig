<form method="post" action="{{ path('app_vaccination_delete', {'id': vaccination.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vaccination.id) }}">
    <button class="btn">Delete</button>
</form>
