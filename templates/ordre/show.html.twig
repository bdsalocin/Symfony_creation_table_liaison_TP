{% extends 'base.html.twig' %}

{% block title %}Ordre{% endblock %}

{% block body %}
    <h1>Ordre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordre.id }}</td>
            </tr>
            <tr>
                <th>TypeOrdre</th>
                <td>{{ ordre.typeOrdre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ordre_index') }}">back to list</a>

    <a href="{{ path('app_ordre_edit', {'id': ordre.id}) }}">edit</a>

    {{ include('ordre/_delete_form.html.twig') }}
{% endblock %}
