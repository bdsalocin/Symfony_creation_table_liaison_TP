{% extends 'base.html.twig' %}

{% block title %}ListeVaccin{% endblock %}

{% block body %}
    <h1>ListeVaccin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_vaccin.id }}</td>
            </tr>
            <tr>
                <th>NomVaccin</th>
                <td>{{ liste_vaccin.nomVaccin }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_vaccin_index') }}">back to list</a>

    <a href="{{ path('app_liste_vaccin_edit', {'id': liste_vaccin.id}) }}">edit</a>

    {{ include('liste_vaccin/_delete_form.html.twig') }}
{% endblock %}
