<form method="post" action="{{ path('app_liste_vaccin_delete', {'id': liste_vaccin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_vaccin.id) }}">
    <button class="btn">Delete</button>
</form>
