<form method="post" action="{{ path('app_adoptant_animal_delete', {'id': adoptant_animal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adoptant_animal.id) }}">
    <button class="btn">Delete</button>
</form>
