<form method="post" action="{{ path('app_comportement_delete', {'id': comportement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comportement.id) }}">
    <button class="btn">Delete</button>
</form>
