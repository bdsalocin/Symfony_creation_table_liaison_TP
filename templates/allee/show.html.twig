{% extends 'base.html.twig' %}

{% block title %}Allee{% endblock %}

{% block body %}
    <h1>Allee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ allee.id }}</td>
            </tr>
            <tr>
                <th>NumeroAllee</th>
                <td>{{ allee.numeroAllee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_allee_index') }}">back to list</a>

    <a href="{{ path('app_allee_edit', {'id': allee.id}) }}">edit</a>

    {{ include('allee/_delete_form.html.twig') }}
{% endblock %}
