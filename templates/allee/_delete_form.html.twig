<form method="post" action="{{ path('app_allee_delete', {'id': allee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ allee.id) }}">
    <button class="btn">Delete</button>
</form>
